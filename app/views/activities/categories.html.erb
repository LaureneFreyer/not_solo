<% @categories.each do |category| %>
  <%= link_to activities_by_category_path(category), class: 'btn btn-primary' do  %>
  <%= category %>
  <i class="<%= Activity::CATEGORY_ICONS[category] %>"></i>
  <% end %>
<% end %>
